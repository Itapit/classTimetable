import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'class-timetable',
  webDir: 'dist/classTimetable'
};

export default config;
